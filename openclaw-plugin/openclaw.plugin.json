{
  "id": "mnemo",
  "name": "Mnemo Memory",
  "description": "AI agent memory — server mode (mnemo-server). Hybrid vector + keyword search.",
  "kind": "memory",
  "configSchema": {
    "type": "object",
    "properties": {
      "apiUrl": {
        "type": "string",
        "description": "mnemo-server URL (server mode)"
      },
      "apiToken": {
        "type": "string",
        "description": "API token for mnemo-server authentication (preferred over userToken)"
      },
      "userToken": {
        "type": "string",
        "description": "User token (legacy alias for apiToken; use apiToken for new setups)"
      }
    }
  },
  "uiHints": {
    "apiUrl": {
      "label": "Server URL",
      "placeholder": "https://your-server.example.com"
    },
    "apiToken": {
      "label": "API Token",
      "placeholder": "mnemo_...",
      "sensitive": true
    },
    "userToken": {
      "label": "User Token",
      "placeholder": "mnemo_...",
      "sensitive": true
    }
  }
}
